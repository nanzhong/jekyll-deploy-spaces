RSpec.describe Jekyll::Deploy::Spaces do
  it 'has a version number' do
    expect(Jekyll::Deploy::Spaces::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
